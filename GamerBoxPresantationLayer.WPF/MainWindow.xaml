<Window x:Class="GamerBoxPresantationLayer.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GamerBoxPresantationLayer.WPF"
        mc:Ignorable="d"
        Title="GamerBox" Height="850" Width="1280"
        Background="{DynamicResource MainBackgroundBrush}" 
        WindowStartupLocation="CenterScreen" WindowStyle="None" ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style x:Key="WindowBtnStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseBtnStyle" TargetType="Button" BasedOn="{StaticResource WindowBtnStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="{DynamicResource PanelBackgroundBrush}" Grid.Column="0" MouseDown="Header_MouseDown" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Margin="0,40,0,20" Grid.Row="0">
                    <TextBlock Text="🎮 GAMERBOX" FontSize="32" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" 
                               HorizontalAlignment="Center" Width="Auto"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <Button Content="Home" x:Name="btnHome" Click="btnHomeClick" Style="{DynamicResource MenuButtons}"/>
                    <Button Content="Reviews" x:Name="btnRvs" Click="btnRvsClick" Style="{DynamicResource MenuButtons}" />
                    <Button Content="Lists" x:Name="btnlist" Click="btnListClick" Style="{DynamicResource MenuButtons}" />
                    <Button Content="Watchlist" x:Name="btnWtList" Click="btnWtListClick" Style="{DynamicResource MenuButtons}"/>
                    <Button Content="Profile" x:Name="btnProfile" Click="btnProfileClick" Style="{DynamicResource MenuButtons}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,0,20">
                    <Border x:Name="versionBrd" CornerRadius="10" Height="24" Width="140" HorizontalAlignment="Center">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource BorderBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Label Content="v1.0.0" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" 
                               HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Background="Transparent" MouseDown="Header_MouseDown">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,10,10,0">

                    <Button x:Name="btnThemeToggle" Click="btnThemeToggle_Click" 
                            Content="🌓" ToolTip="Temayı Değiştir"
                            Width="32" Height="32" Margin="0,0,10,0"
                            Background="Transparent" BorderThickness="0" FontSize="16" Foreground="{DynamicResource PrimaryTextBrush}"/>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button Content="Giriş Yap" Click="btnSignInCli" Height="28" Width="100" 
                                Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0" FontWeight="SemiBold">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLoggedIn}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <StackPanel x:Name="profilePanel" Orientation="Horizontal" VerticalAlignment="Center">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLoggedIn}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <TextBlock Text="{Binding UserName}" Foreground="{DynamicResource AccentBrush}" FontSize="14" 
                                       FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>

                            <Button Content="Çıkış" Click="btnSignOut_Click" Width="50" Height="24" FontSize="11" 
                                    Background="{DynamicResource BorderBrush}" Foreground="{DynamicResource PrimaryTextBrush}" BorderThickness="0"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button Content="&#xE921;" x:Name="btnStb" Click="btnStbClick" Style="{StaticResource WindowBtnStyle}" ToolTip="Simge Durumu"/>
                        <Button Content="&#xE922;" x:Name="btnFullSc" Click="btnFullScClick" Style="{StaticResource WindowBtnStyle}" ToolTip="Tam Ekran"/>
                        <Button Content="&#xE8BB;" x:Name="btnClose" Click="btnCloseClick" Style="{StaticResource CloseBtnStyle}" ToolTip="Kapat"/>
                    </StackPanel>

                </StackPanel>
            </Grid>

            <ContentControl x:Name="MainContent" Grid.Row="1" Margin="0"/>

        </Grid>
    </Grid>
</Window>