<Window x:Class="GamerBoxPresantationLayer.WPF.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mesaj" Height="200" Width="400"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="{DynamicResource CardBackgroundBrush}" 
            BorderBrush="{DynamicResource AccentBrush}" 
            BorderThickness="1" 
            CornerRadius="10">

        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="lblTitle" Text="Başlık" 
                       Foreground="{DynamicResource AccentBrush}" 
                       FontSize="18" FontWeight="Bold"/>

            <TextBlock x:Name="lblMessage" Grid.Row="1" 
                       Text="Buraya mesaj gelecek..." 
                       Foreground="{DynamicResource PrimaryTextBrush}" 
                       TextWrapping="Wrap" 
                       VerticalAlignment="Center" 
                       FontSize="14" Margin="0,10"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">

                <Button x:Name="btnCancel" Content="İptal" Click="btnCancel_Click"
                        Width="80" Height="30" Margin="0,0,10,0"
                        Background="Transparent" 
                        Foreground="{DynamicResource SecondaryTextBrush}"
                        BorderBrush="{DynamicResource SecondaryTextBrush}"
                        BorderThickness="1">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button x:Name="btnOk" Content="Tamam" Click="btnOk_Click"
                        Width="80" Height="30"
                        Background="{DynamicResource AccentBrush}" 
                        Foreground="White" FontWeight="Bold"
                        BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>