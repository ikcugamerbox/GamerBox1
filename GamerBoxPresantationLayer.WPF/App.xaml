<Application x:Class="GamerBoxPresantationLayer.WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:GamerBoxPresantationLayer.WPF"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

     
        
        <Style x:Key="MiniButtons" TargetType="Button">
            <Setter Property="Foreground" Value="DimGray"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#121212"/>
            <Setter Property="BorderThickness" Value="0,0,0,0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border"
                                BorderThickness="0,0,0,0"
                                Background="#121212">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="DarkSlateGray"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        
        <Style x:Key="MenuButtons" TargetType="Button">
            <Setter Property="Margin" Value="0,0,0,1"/>
            <Setter Property="Foreground" Value="DimGray"/>
            <Setter Property="Height" Value="64"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#121212"/>
            <Setter Property="BorderThickness" Value="5,0,0,0"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border"
                        BorderBrush="{x:Null}"
                        BorderThickness="5,0,0,0"
                        Background="{x:Null}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="DarkSlateGray"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="DarkSlateGray"/>
                               
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="Background" Value="DarkSlateGray"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="Wheat"/>

                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!--Signinbutton-->
        <Style x:Key="SignInButton" TargetType="Button">
            <Setter Property="Margin" Value="5,5,5,5"/>
            <Setter Property="Foreground" Value="#42BC88"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Width" Value="72"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#FF153222"/>
            <Setter Property="BorderThickness" Value="2,2,2,2"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Name="border">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#42BC88"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#42BC88"/>

                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#42BC88"/>
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="Wheat"/>

                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="TextboxStyle" TargetType="{x:Type TextBox}">
            
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="Wheat"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="DarkSlateGray"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Bisque"/>
                    <Setter Property="BorderBrush" Value="BlanchedAlmond"/>


                </Trigger>

                </Style.Triggers>

            
        </Style>

        <Style x:Key="PasswordBoxStyle" TargetType="{x:Type PasswordBox}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="Wheat"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="BorderBrush" Value="DarkSlateGray"/>
            <Setter Property="Padding" Value="4"/>

            <Style.Triggers>

                <!-- Hover -->
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="Bisque"/>
                    <Setter Property="BorderBrush" Value="BlanchedAlmond"/>
                </Trigger>

                <!-- Focus -->
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="#FF8B6F00"/>
                    <Setter Property="Background" Value="#FFF4DAA1"/>
                </Trigger>

            </Style.Triggers>
        </Style>






    </Application.Resources>
</Application>
