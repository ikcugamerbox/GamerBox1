<UserControl x:Class="GamerBoxPresantationLayer.WPF.Views.UserControls.UCReviews"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Background="{DynamicResource MainBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Topluluk Akışı" FontSize="24" FontWeight="Bold" 
                   Foreground="{DynamicResource AccentBrush}" Margin="20,20,0,15"/>

        <ScrollViewer Grid.Row="1" Margin="10">
            <ItemsControl ItemsSource="{Binding RecentPosts}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10,0,10,15" Background="{DynamicResource CardBackgroundBrush}" 
                                CornerRadius="10" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Width="40" Height="40" CornerRadius="20" 
                                        Background="{DynamicResource BorderBrush}" VerticalAlignment="Top">
                                    <TextBlock Text="👤" Foreground="{DynamicResource SecondaryTextBrush}" 
                                               HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
                                </Border>

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <DockPanel>
                                        <TextBlock Text="{Binding UserName}" Foreground="{DynamicResource AccentBrush}" 
                                                   FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{Binding GameName}" Foreground="{DynamicResource SecondaryTextBrush}" 
                                                   FontSize="12" Margin="10,2,0,0" FontStyle="Italic"/>
                                        <TextBlock Text="{Binding DateStr}" Foreground="{DynamicResource MutedTextBrush}" 
                                                   DockPanel.Dock="Right" HorizontalAlignment="Right" FontSize="11"/>
                                    </DockPanel>

                                    <TextBlock Text="{Binding Content}" Foreground="{DynamicResource PrimaryTextBrush}" 
                                               Margin="0,5,0,5" TextWrapping="Wrap"/>

                                    <TextBlock Text="{Binding HashtagsStr}" Foreground="{DynamicResource AccentBrush}" 
                                               FontSize="12" Opacity="0.8"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>